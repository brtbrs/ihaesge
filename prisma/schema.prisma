generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Prisma schema placeholder for future Next.js integration.
model Placeholder {
  id Int @id @default(autoincrement())
}
